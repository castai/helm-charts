apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pod-pinner.fullname" . }}-test-connection"
  labels:
    {{- include "pod-pinner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pod-pinner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
