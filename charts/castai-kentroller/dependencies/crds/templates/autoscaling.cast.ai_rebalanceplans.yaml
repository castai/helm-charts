# Code generated by controller-gen. DO NOT EDIT.
# Source declarations: ./services/kentroller/gen.go
# Regenerate with: make generate
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: rebalanceplans.autoscaling.cast.ai
spec:
  group: autoscaling.cast.ai
  names:
    kind: RebalancePlan
    listKind: RebalancePlanList
    plural: rebalanceplans
    shortNames:
      - rp
    singular: rebalanceplan
  scope: Cluster
  versions:
    - additionalPrinterColumns:
        - description: Current monthly cost
          jsonPath: .status.savings.currentMonthlyCost
          name: Before
          type: string
        - description: Projected monthly cost after rebalancing
          jsonPath: .status.savings.projectedMonthlyCost
          name: After
          type: string
        - description: Projected percentage savings
          jsonPath: .status.savings.projectedSavingsPercent
          name: Savings%
          type: string
        - description: State of the rebalancing plan
          jsonPath: .status.state
          name: State
          type: string
        - description: Auto executed plan or not
          jsonPath: .spec.execute
          name: Auto-executed
          type: boolean
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha
      schema:
        openAPIV3Schema:
          description: RebalancePlan is the Schema for the rebalanceplans API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: spec defines the desired state of RebalancePlan
              properties:
                aggressiveMode:
                  description: |-
                    AggressiveMode is an optional rebalancer execution-level configuration to skip or ignore certain constraints.
                    It differs from RebalancePlanClaim aggressive mode because this is a subset of execution-level, not generation, options only.
                  properties:
                    drainTimeout:
                      description: |-
                        DrainTimeout specifies the drain timeout for aggressive eviction.
                        If not specified, no drain timeout is set (node drains indefinitely until complete).
                        Format: duration string (e.g., "5m", "10m", "30m")
                      type: string
                    ignoreZeroNodePoolDisruptionBudgets:
                      description: |-
                        IgnoreZeroNodePoolDisruptionBudgets overrides NodePool disruption budgets that allow 0
                        disruptions with the default disruption budget (10% of nodes). This prevents nodes under
                        active zero-disruption budgets from blocking rebalancing.
                        WARNING: May violate NodePool disruption budget guarantees.
                      type: boolean
                  type: object
                execute:
                  default: false
                  description: |-
                    Execute determines whether this plan should be executed.
                    When false (default), the plan is visible but execution is skipped (pending approval).
                    When true, the plan will be executed immediately.
                  type: boolean
                executionPolicy:
                  description: ExecutionPolicy defines policies for execution validation, propagated from RebalancePlanClaim.
                  properties:
                    achievedSavingsPercentageThreshold:
                      description: |-
                        AchievedSavingsPercentageThreshold is the minimum percentage of cost savings relative to the
                        original (blue) cost that must be achieved for the rebalancing to proceed.
                        Range: 0-100
                        0   = No validation (optimistic, always proceed)
                        80  = Require at least 80% of the original cost to be saved
                        100 = Require exact cost prediction (strict, may fail often)
                        The rebalancing plan will not proceed after creating the nodes if the achieved savings
                        percentage is below this threshold.
                      format: int32
                      maximum: 100
                      minimum: 0
                      type: integer
                  type: object
                nodeClaimsToAdd:
                  items:
                    properties:
                      metadata:
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            type: object
                          name:
                            type: string
                        required:
                          - name
                        type: object
                      spec:
                        description: NodeClaimSpec describes the desired state of the NodeClaim
                        properties:
                          expireAfter:
                            default: 720h
                            description: |-
                              ExpireAfter is the duration the controller will wait
                              before terminating a node, measured from when the node is created. This
                              is useful to implement features like eventually consistent node upgrade,
                              memory leak protection, and disruption testing.
                            pattern: ^(([0-9]+(s|m|h))+|Never)$
                            type: string
                          nodeClassRef:
                            description: NodeClassRef is a reference to an object that defines provider specific configuration
                            properties:
                              group:
                                description: API version of the referent
                                pattern: ^[^/]*$
                                type: string
                              kind:
                                description: 'Kind of the referent; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"'
                                type: string
                              name:
                                description: 'Name of the referent; More info: http://kubernetes.io/docs/user-guide/identifiers#names'
                                type: string
                            required:
                              - group
                              - kind
                              - name
                            type: object
                          requirements:
                            description: Requirements are layered with GetLabels and applied to every node.
                            items:
                              description: |-
                                A node selector requirement is a selector that contains values, a key, an operator that relates the key and values
                                and minValues that represent the requirement to have at least that many values.
                              properties:
                                key:
                                  description: The label key that the selector applies to.
                                  type: string
                                minValues:
                                  description: |-
                                    This field is ALPHA and can be dropped or replaced at any time
                                    MinValues is the minimum number of unique values required to define the flexibility of the specific requirement.
                                  maximum: 50
                                  minimum: 1
                                  type: integer
                                operator:
                                  description: |-
                                    Represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists, DoesNotExist. Gt, Lt, Gte, and Lte.
                                  type: string
                                values:
                                  description: |-
                                    An array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. If the operator is Gt, Lt, Gte, or Lte, the values
                                    array must have a single element, which will be interpreted as an integer.
                                    This array is replaced during a strategic merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                                - key
                                - operator
                              type: object
                            maxItems: 100
                            type: array
                          resources:
                            description: Resources models the resource requirements for the NodeClaim to launch
                            properties:
                              requests:
                                additionalProperties:
                                  anyOf:
                                    - type: integer
                                    - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: Requests describes the minimum required resources for the NodeClaim to launch
                                type: object
                            type: object
                          startupTaints:
                            description: |-
                              StartupTaints are taints that are applied to nodes upon startup which are expected to be removed automatically
                              within a short period of time, typically by a DaemonSet that tolerates the taint. These are commonly used by
                              daemonsets to allow initialization and enforce startup ordering.  StartupTaints are ignored for provisioning
                              purposes in that pods are not required to tolerate a StartupTaint in order to have nodes provisioned for them.
                            items:
                              description: |-
                                The node this Taint is attached to has the "effect" on
                                any pod that does not tolerate the Taint.
                              properties:
                                effect:
                                  description: |-
                                    Required. The effect of the taint on pods
                                    that do not tolerate the taint.
                                    Valid effects are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: Required. The taint key to be applied to a node.
                                  type: string
                                timeAdded:
                                  description: TimeAdded represents the time at which the taint was added.
                                  format: date-time
                                  type: string
                                value:
                                  description: The taint value corresponding to the taint key.
                                  type: string
                              required:
                                - effect
                                - key
                              type: object
                            type: array
                          taints:
                            description: Taints will be applied to the NodeClaim's node.
                            items:
                              description: |-
                                The node this Taint is attached to has the "effect" on
                                any pod that does not tolerate the Taint.
                              properties:
                                effect:
                                  description: |-
                                    Required. The effect of the taint on pods
                                    that do not tolerate the taint.
                                    Valid effects are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: Required. The taint key to be applied to a node.
                                  type: string
                                timeAdded:
                                  description: TimeAdded represents the time at which the taint was added.
                                  format: date-time
                                  type: string
                                value:
                                  description: The taint value corresponding to the taint key.
                                  type: string
                              required:
                                - effect
                                - key
                              type: object
                            type: array
                          terminationGracePeriod:
                            description: |-
                              TerminationGracePeriod is the maximum duration the controller will wait before forcefully deleting the pods on a node, measured from when deletion is first initiated.

                              Warning: this feature takes precedence over a Pod's terminationGracePeriodSeconds value, and bypasses any blocked PDBs or the karpenter.sh/do-not-disrupt annotation.

                              This field is intended to be used by cluster administrators to enforce that nodes can be cycled within a given time period.
                              When set, drifted nodes will begin draining even if there are pods blocking eviction. Draining will respect PDBs and the do-not-disrupt annotation until the TGP is reached.

                              Karpenter will preemptively delete pods so their terminationGracePeriodSeconds align with the node's terminationGracePeriod.
                              If a pod would be terminated without being granted its full terminationGracePeriodSeconds prior to the node timeout,
                              that pod will be deleted at T = node timeout - pod terminationGracePeriodSeconds.

                              The feature can also be used to allow maximum time limits for long-running jobs which can delay node termination with preStop hooks.
                              If left undefined, the controller will wait indefinitely for pods to be drained.
                            pattern: ^([0-9]+(s|m|h))+$
                            type: string
                        required:
                          - nodeClassRef
                          - requirements
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                      - metadata
                      - spec
                    type: object
                  type: array
                nodeClaimsToDelete:
                  items:
                    properties:
                      nodeClaimName:
                        type: string
                    required:
                      - nodeClaimName
                    type: object
                  type: array
                podsToMigrate:
                  items:
                    properties:
                      dstNodeClaimName:
                        type: string
                      name:
                        type: string
                      namespace:
                        type: string
                      policy:
                        default: Optional
                        description: |-
                          Policy specifies whether the migration should be mandatory or optional.
                          Mandatory policy forbids eviction, so if the migration fails, the node drain is considered failed.
                          Optional policy allows the pod to be evicted if the migration fails.
                        type: string
                      srcNode:
                        type: string
                    required:
                      - dstNodeClaimName
                      - name
                      - namespace
                      - srcNode
                    type: object
                  type: array
                timeouts:
                  description: Timeouts controls timeout settings for plan execution
                  properties:
                    rebalanceExecutionTimeout:
                      description: |-
                        RebalanceExecutionTimeout is the maximum time for plan execution
                        Format: duration string (e.g., "1h", "30m", "1h30m")
                      type: string
                  type: object
              type: object
            status:
              description: status defines the observed state of RebalancePlan
              properties:
                achievedOutcome:
                  description: |-
                    AchievedOutcome captures the actual execution result once a plan reaches a terminal state
                    (Done or Failed). Computed by kentroller after all NodeClaims settle;
                    kent reads this field verbatim without re-computing arithmetic.
                    Nil while the plan is still running, pending, or for deletion-only plans with no new nodes.
                  properties:
                    clusterTotals:
                      description: |-
                        ClusterTotals holds cluster-level cost aggregates adjusted for the actual
                        achieved node costs (accounts for ICE fallback price differences).
                      properties:
                        memoryMiB:
                          description: MemoryMiB is the total memory in mebibytes.
                          format: int64
                          type: integer
                        milliCPU:
                          description: MilliCPU is the total CPU in millicores.
                          format: int64
                          type: integer
                        nodes:
                          description: Nodes is the count of nodes.
                          format: int32
                          type: integer
                        priceHourly:
                          description: PriceHourly is the total hourly cost as a decimal string.
                          type: string
                        priceMonthly:
                          description: PriceMonthly is the total monthly cost as a decimal string (hourly * 730).
                          type: string
                      type: object
                    diff:
                      description: |-
                        Diff is the achieved cost difference: blue cost minus achieved green cost.
                        Not set when the plan failed before any blue nodes were deleted (FailurePhaseCreation).
                      properties:
                        clusterSavingsPercentage:
                          description: ClusterSavingsPercentage is the savings as a percentage of the total cluster monthly cost.
                          type: string
                        priceHourly:
                          description: PriceHourly is the hourly savings (blue hourly − achieved green hourly).
                          type: string
                        priceMonthly:
                          description: PriceMonthly is the monthly savings (blue monthly − achieved green monthly).
                          type: string
                        savingsPercentage:
                          description: SavingsPercentage is the savings as a percentage of the blue (replaced) nodes monthly cost.
                          type: string
                      type: object
                    failedNodes:
                      description: FailedNodes are NodeClaims where every provisioning attempt failed.
                      items:
                        description: AchievedFailedNode describes a node where all provisioning attempts failed.
                        properties:
                          attempts:
                            description: Attempts lists each failed attempt with the instance type tried and error description.
                            items:
                              description: AchievedFailedAttempt describes one failed provisioning attempt.
                              properties:
                                description:
                                  description: Description is the failure reason (e.g. "InsufficientCapacityError").
                                  type: string
                                instanceType:
                                  description: InstanceType is the instance type that was attempted.
                                  type: string
                              required:
                                - description
                                - instanceType
                              type: object
                            type: array
                          name:
                            description: Name is the planned node name.
                            type: string
                        required:
                          - name
                        type: object
                      type: array
                    nodes:
                      description: |-
                        Nodes are the NodeClaims successfully provisioned. For ICE fallbacks the
                        InstanceType reflects the actual type used, not the originally planned one.
                      items:
                        description: AchievedNodeDetail describes a single successfully provisioned node.
                        properties:
                          instanceType:
                            description: InstanceType is the actual instance type provisioned (may differ from plan on ICE).
                            type: string
                          isSpot:
                            description: IsSpot indicates whether this is a spot/preemptible instance.
                            type: boolean
                          memoryMiB:
                            description: MemoryMiB is the memory capacity in mebibytes, carried forward from the plan.
                            format: int64
                            type: integer
                          milliCPU:
                            description: MilliCPU is the CPU capacity in millicores, carried forward from the plan.
                            format: int64
                            type: integer
                          name:
                            description: Name is the planned node name (base name from spec.nodeClaimsToAdd).
                            type: string
                          priceHourly:
                            description: PriceHourly is the hourly cost of the provisioned instance type as a decimal string.
                            type: string
                        required:
                          - name
                        type: object
                      type: array
                    totals:
                      description: Totals aggregates cost/resource totals for successfully provisioned nodes.
                      properties:
                        memoryMiB:
                          description: MemoryMiB is the total memory in mebibytes.
                          format: int64
                          type: integer
                        milliCPU:
                          description: MilliCPU is the total CPU in millicores.
                          format: int64
                          type: integer
                        nodes:
                          description: Nodes is the count of nodes.
                          format: int32
                          type: integer
                        priceHourly:
                          description: PriceHourly is the total hourly cost as a decimal string.
                          type: string
                        priceMonthly:
                          description: PriceMonthly is the total monthly cost as a decimal string (hourly * 730).
                          type: string
                      type: object
                  type: object
                conditions:
                  description: |-
                    conditions represent the current state of the RebalancePlan resource.
                    Each condition has a unique type and reflects the status of a specific aspect of the resource.

                    Standard condition types include:
                    - "Available": the resource is fully functional
                    - "Progressing": the resource is being created or updated
                    - "Degraded": the resource failed to reach or maintain its desired state

                    The status of each condition is one of True, False, or Unknown.
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                failurePhase:
                  description: |-
                    FailurePhase records which phase of rebalancing caused a transition to Failed state.
                    Used by the controller on restart to perform the correct cleanup without re-inferring
                    the phase from node status (which may have changed after the failure).
                  enum:
                    - Creation
                    - Deletion
                  type: string
                nodesCreation:
                  additionalProperties:
                    description: NodeCreationStatus tracks the status of a single node being added during rebalance
                    properties:
                      attempts:
                        description: |-
                          Attempts is the history of all attempts for this node
                          Records all attempts with their status (in-progress, success, failed)
                          The latest attempt represents the current state
                        items:
                          description: AttemptRecord represents a single attempt to create a NodeClaim
                          properties:
                            capacityType:
                              description: CapacityType is the capacity type for this attempt (e.g., "spot", "on-demand")
                              type: string
                            description:
                              description: Description provides details about this attempt (e.g., "InsufficientCapacityError")
                              type: string
                            instanceType:
                              description: InstanceType is the instance type that was attempted
                              type: string
                            nodeClaimName:
                              description: NodeClaimName is the name of the NodeClaim for this attempt (includes -attempt-N suffix)
                              type: string
                            status:
                              description: Status describes the outcome of this attempt
                              enum:
                                - InProgress
                                - Success
                                - Failed
                              type: string
                            timestamp:
                              description: Timestamp is when this attempt was recorded
                              format: date-time
                              type: string
                            zone:
                              description: |-
                                Zone is the account-local availability zone name for this attempt.

                                Deprecated: use ZoneID for offering identity. Zone may be removed in a future API version.
                              type: string
                            zoneId:
                              description: |-
                                ZoneID is the provider-stable availability zone identifier for this attempt.
                                For AWS this is the physical AZ ID, for example "use1-az1".
                              type: string
                          required:
                            - description
                            - instanceType
                            - nodeClaimName
                            - status
                            - timestamp
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      name:
                        description: Name is the base name from Spec.NodeClaimsToAdd[].Metadata.Name
                        type: string
                    required:
                      - name
                    type: object
                  description: NodesCreation tracks the status of NodeClaims being added, keyed by node name
                  type: object
                nodesDeletion:
                  additionalProperties:
                    description: NodeDeletionStatus tracks the status of a single node being deleted during rebalance
                    properties:
                      events:
                        description: |-
                          Events is the history of all deletion events for this node
                          Records all state changes (in-progress, success, failed)
                          The latest event represents the current state
                        items:
                          description: DeletionEvent represents a single deletion event
                          properties:
                            description:
                              description: Description provides details about this event (e.g., error messages)
                              type: string
                            migrationPlanName:
                              description: |-
                                MigrationPlanName is the name of the RebalanceMigrationPlan associated with this event.
                                Only set when Status is LiveMigrationCreated.
                              type: string
                            status:
                              description: Status describes the state of the deletion at this event
                              enum:
                                - LiveMigrationCreated
                                - LiveMigrationInProgress
                                - LiveMigrationSucceeded
                                - LiveMigrationFailed
                                - InProgress
                                - Success
                                - Failed
                              type: string
                            timestamp:
                              description: Timestamp is when this event was recorded
                              format: date-time
                              type: string
                          required:
                            - status
                            - timestamp
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      name:
                        description: Name is the name of the NodeClaim being deleted
                        type: string
                    required:
                      - name
                    type: object
                  description: NodesDeletion tracks the status of NodeClaims being deleted, keyed by node name
                  type: object
                savings:
                  description: |-
                    Savings represents the estimated cost savings from executing this rebalancing plan.
                    This is computed by Kent based on the difference between current and proposed node configurations.
                  properties:
                    blueNodes:
                      description: BlueNodes contains per-node resource and cost details for nodes being removed.
                      items:
                        description: NodeCostDetails carries resolved resource and cost details for a single node in a rebalancing plan.
                        properties:
                          instanceType:
                            description: InstanceType is the resolved instance type (e.g., "m5.xlarge").
                            type: string
                          isSpot:
                            description: IsSpot indicates whether this is a spot/preemptible instance.
                            type: boolean
                          memoryMiB:
                            description: MemoryMiB is the memory capacity in mebibytes.
                            format: int64
                            type: integer
                          milliCPU:
                            description: MilliCPU is the CPU capacity in millicores (e.g., 4000 for 4 vCPUs).
                            format: int64
                            type: integer
                          name:
                            description: Name is the node or NodeClaim name.
                            type: string
                          priceHourly:
                            description: PriceHourly is the hourly cost as a decimal string (e.g., "0.192").
                            type: string
                        required:
                          - name
                        type: object
                      type: array
                    currency:
                      default: USD
                      description: Currency is the currency code for all cost amounts (e.g., "USD", "EUR").
                      type: string
                    currentClusterHourlyCost:
                      description: |-
                        CurrentClusterHourlyCost is the estimated hourly cost of the entire current cluster.
                        Format: decimal string (e.g., "3.42").
                      type: string
                    currentClusterMonthlyCost:
                      description: |-
                        CurrentClusterMonthlyCost is the estimated monthly cost of the entire current cluster.
                        Format: decimal string (e.g., "2500.00").
                      type: string
                    currentClusterNodes:
                      description: CurrentClusterNodes is the total number of nodes in the cluster before rebalancing.
                      format: int32
                      type: integer
                    currentHourlyCost:
                      description: |-
                        CurrentHourlyCost is the estimated hourly cost of the current (blue) rebalanced nodes.
                        Format: decimal string (e.g., "1.69").
                      type: string
                    currentMigratablePods:
                      description: CurrentMigratablePods is the number of pods that can be migrated during rebalancing.
                      format: int32
                      type: integer
                    currentMonthlyCost:
                      description: |-
                        CurrentMonthlyCost is the estimated monthly cost of the current (blue) rebalanced nodes.
                        Format: decimal string (e.g., "1234.56").
                      type: string
                    currentProblematicPods:
                      description: CurrentProblematicPods is the number of pods with issues preventing migration.
                      format: int32
                      type: integer
                    currentReplaceableNodes:
                      description: CurrentReplaceableNodes is the number of nodes selected for rebalancing (blue nodes).
                      format: int32
                      type: integer
                    greenNodes:
                      description: GreenNodes contains per-node resource and cost details for nodes being added.
                      items:
                        description: NodeCostDetails carries resolved resource and cost details for a single node in a rebalancing plan.
                        properties:
                          instanceType:
                            description: InstanceType is the resolved instance type (e.g., "m5.xlarge").
                            type: string
                          isSpot:
                            description: IsSpot indicates whether this is a spot/preemptible instance.
                            type: boolean
                          memoryMiB:
                            description: MemoryMiB is the memory capacity in mebibytes.
                            format: int64
                            type: integer
                          milliCPU:
                            description: MilliCPU is the CPU capacity in millicores (e.g., 4000 for 4 vCPUs).
                            format: int64
                            type: integer
                          name:
                            description: Name is the node or NodeClaim name.
                            type: string
                          priceHourly:
                            description: PriceHourly is the hourly cost as a decimal string (e.g., "0.192").
                            type: string
                        required:
                          - name
                        type: object
                      type: array
                    projectedClusterHourlyCost:
                      description: |-
                        ProjectedClusterHourlyCost is the estimated hourly cost of the entire cluster after rebalancing.
                        Format: decimal string (e.g., "3.25").
                      type: string
                    projectedClusterMonthlyCost:
                      description: |-
                        ProjectedClusterMonthlyCost is the estimated monthly cost of the entire cluster after rebalancing.
                        Format: decimal string (e.g., "2376.55").
                      type: string
                    projectedClusterNodes:
                      description: ProjectedClusterNodes is the total number of nodes in the cluster after rebalancing.
                      format: int32
                      type: integer
                    projectedClusterSavingsPercent:
                      description: |-
                        ProjectedClusterSavingsPercent is the percentage of cost reduction relative to total cluster spend.
                        Format: decimal string (e.g., "10.2" means 10.2% cluster savings).
                      type: string
                    projectedGreenNodes:
                      description: ProjectedGreenNodes is the number of new nodes added during rebalancing (green nodes).
                      format: int32
                      type: integer
                    projectedHourlyCost:
                      description: |-
                        ProjectedHourlyCost is the estimated hourly cost of the new (green) rebalanced nodes.
                        Format: decimal string (e.g., "1.52").
                      type: string
                    projectedMonthlyCost:
                      description: |-
                        ProjectedMonthlyCost is the estimated monthly cost of the new (green) rebalanced nodes.
                        Format: decimal string (e.g., "1111.11").
                      type: string
                    projectedSavingsCostHourly:
                      description: |-
                        ProjectedSavingsCostHourly is the estimated hourly cost savings from executing this plan.
                        Format: decimal string (e.g., "0.17", "-0.07").
                      type: string
                    projectedSavingsCostMonthly:
                      description: |-
                        ProjectedSavingsCostMonthly is the estimated monthly cost savings from executing this plan.
                        Positive values indicate cost reduction, negative values indicate cost increase.
                        This is calculated as: CurrentMonthlyCost - ProjectedMonthlyCost.
                        Format: decimal string (e.g., "123.45", "-50.00").
                      type: string
                    projectedSavingsPercent:
                      description: |-
                        ProjectedSavingsPercent is the percentage of cost reduction relative to current spend.
                        Format: decimal string (e.g., "15.5" means 15.5% savings).
                      type: string
                  type: object
                state:
                  enum:
                    - Pending
                    - Running
                    - Done
                    - Failed
                    - Created
                  type: string
                validatedSavings:
                  description: |-
                    ValidatedSavings represents the savings used for ExecutionPolicy threshold validation,
                    computed after new NodeClaims are provisioned but before old nodes are deleted.
                    Costs are based on planned (pre-provisioning) pricing; if a fallback instance type was used
                    due to InsufficientCapacityError, the originally planned price is used as a conservative
                    estimate. This field reflects validation inputs, not actual realized savings.
                  properties:
                    blueNodes:
                      description: BlueNodes contains per-node resource and cost details for nodes being removed.
                      items:
                        description: NodeCostDetails carries resolved resource and cost details for a single node in a rebalancing plan.
                        properties:
                          instanceType:
                            description: InstanceType is the resolved instance type (e.g., "m5.xlarge").
                            type: string
                          isSpot:
                            description: IsSpot indicates whether this is a spot/preemptible instance.
                            type: boolean
                          memoryMiB:
                            description: MemoryMiB is the memory capacity in mebibytes.
                            format: int64
                            type: integer
                          milliCPU:
                            description: MilliCPU is the CPU capacity in millicores (e.g., 4000 for 4 vCPUs).
                            format: int64
                            type: integer
                          name:
                            description: Name is the node or NodeClaim name.
                            type: string
                          priceHourly:
                            description: PriceHourly is the hourly cost as a decimal string (e.g., "0.192").
                            type: string
                        required:
                          - name
                        type: object
                      type: array
                    currency:
                      default: USD
                      description: Currency is the currency code for all cost amounts (e.g., "USD", "EUR").
                      type: string
                    currentClusterHourlyCost:
                      description: |-
                        CurrentClusterHourlyCost is the estimated hourly cost of the entire current cluster.
                        Format: decimal string (e.g., "3.42").
                      type: string
                    currentClusterMonthlyCost:
                      description: |-
                        CurrentClusterMonthlyCost is the estimated monthly cost of the entire current cluster.
                        Format: decimal string (e.g., "2500.00").
                      type: string
                    currentClusterNodes:
                      description: CurrentClusterNodes is the total number of nodes in the cluster before rebalancing.
                      format: int32
                      type: integer
                    currentHourlyCost:
                      description: |-
                        CurrentHourlyCost is the estimated hourly cost of the current (blue) rebalanced nodes.
                        Format: decimal string (e.g., "1.69").
                      type: string
                    currentMigratablePods:
                      description: CurrentMigratablePods is the number of pods that can be migrated during rebalancing.
                      format: int32
                      type: integer
                    currentMonthlyCost:
                      description: |-
                        CurrentMonthlyCost is the estimated monthly cost of the current (blue) rebalanced nodes.
                        Format: decimal string (e.g., "1234.56").
                      type: string
                    currentProblematicPods:
                      description: CurrentProblematicPods is the number of pods with issues preventing migration.
                      format: int32
                      type: integer
                    currentReplaceableNodes:
                      description: CurrentReplaceableNodes is the number of nodes selected for rebalancing (blue nodes).
                      format: int32
                      type: integer
                    greenNodes:
                      description: GreenNodes contains per-node resource and cost details for nodes being added.
                      items:
                        description: NodeCostDetails carries resolved resource and cost details for a single node in a rebalancing plan.
                        properties:
                          instanceType:
                            description: InstanceType is the resolved instance type (e.g., "m5.xlarge").
                            type: string
                          isSpot:
                            description: IsSpot indicates whether this is a spot/preemptible instance.
                            type: boolean
                          memoryMiB:
                            description: MemoryMiB is the memory capacity in mebibytes.
                            format: int64
                            type: integer
                          milliCPU:
                            description: MilliCPU is the CPU capacity in millicores (e.g., 4000 for 4 vCPUs).
                            format: int64
                            type: integer
                          name:
                            description: Name is the node or NodeClaim name.
                            type: string
                          priceHourly:
                            description: PriceHourly is the hourly cost as a decimal string (e.g., "0.192").
                            type: string
                        required:
                          - name
                        type: object
                      type: array
                    projectedClusterHourlyCost:
                      description: |-
                        ProjectedClusterHourlyCost is the estimated hourly cost of the entire cluster after rebalancing.
                        Format: decimal string (e.g., "3.25").
                      type: string
                    projectedClusterMonthlyCost:
                      description: |-
                        ProjectedClusterMonthlyCost is the estimated monthly cost of the entire cluster after rebalancing.
                        Format: decimal string (e.g., "2376.55").
                      type: string
                    projectedClusterNodes:
                      description: ProjectedClusterNodes is the total number of nodes in the cluster after rebalancing.
                      format: int32
                      type: integer
                    projectedClusterSavingsPercent:
                      description: |-
                        ProjectedClusterSavingsPercent is the percentage of cost reduction relative to total cluster spend.
                        Format: decimal string (e.g., "10.2" means 10.2% cluster savings).
                      type: string
                    projectedGreenNodes:
                      description: ProjectedGreenNodes is the number of new nodes added during rebalancing (green nodes).
                      format: int32
                      type: integer
                    projectedHourlyCost:
                      description: |-
                        ProjectedHourlyCost is the estimated hourly cost of the new (green) rebalanced nodes.
                        Format: decimal string (e.g., "1.52").
                      type: string
                    projectedMonthlyCost:
                      description: |-
                        ProjectedMonthlyCost is the estimated monthly cost of the new (green) rebalanced nodes.
                        Format: decimal string (e.g., "1111.11").
                      type: string
                    projectedSavingsCostHourly:
                      description: |-
                        ProjectedSavingsCostHourly is the estimated hourly cost savings from executing this plan.
                        Format: decimal string (e.g., "0.17", "-0.07").
                      type: string
                    projectedSavingsCostMonthly:
                      description: |-
                        ProjectedSavingsCostMonthly is the estimated monthly cost savings from executing this plan.
                        Positive values indicate cost reduction, negative values indicate cost increase.
                        This is calculated as: CurrentMonthlyCost - ProjectedMonthlyCost.
                        Format: decimal string (e.g., "123.45", "-50.00").
                      type: string
                    projectedSavingsPercent:
                      description: |-
                        ProjectedSavingsPercent is the percentage of cost reduction relative to current spend.
                        Format: decimal string (e.g., "15.5" means 15.5% savings).
                      type: string
                  type: object
                warnings:
                  description: |-
                    Warnings contains warnings from pod placement events during plan generation.
                    Each entry corresponds to a pod that could not be placed.
                  items:
                    description: RebalancePlanWarning describes a warning encountered during plan generation.
                    properties:
                      description:
                        description: Description is a human-readable warning message.
                        type: string
                    required:
                      - description
                    type: object
                  type: array
                  x-kubernetes-list-type: atomic
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
