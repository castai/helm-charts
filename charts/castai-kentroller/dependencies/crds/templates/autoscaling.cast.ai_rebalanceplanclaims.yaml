# Code generated by controller-gen. DO NOT EDIT.
# Source declarations: ./services/kentroller/gen.go
# Regenerate with: make generate
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: rebalanceplanclaims.autoscaling.cast.ai
spec:
  group: autoscaling.cast.ai
  names:
    kind: RebalancePlanClaim
    listKind: RebalancePlanClaimList
    plural: rebalanceplanclaims
    shortNames:
    - rpc
    singular: rebalanceplanclaim
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Current state of the plan request
      jsonPath: .status.state
      name: State
      type: string
    - description: Kent plan ID
      jsonPath: .status.planId
      name: PlanID
      type: string
    - description: Number of selected nodes
      jsonPath: .status.selectedNodesCount
      name: Nodes
      type: integer
    - description: Projected savings percentage
      jsonPath: .status.projectedSavingsPercent
      name: Savings%
      type: string
    - description: Created Rebalance CR
      jsonPath: .status.rebalanceName
      name: Rebalance
      type: string
    - description: Time since creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha
    schema:
      openAPIV3Schema:
        description: |-
          RebalancePlanClaim is the Schema for the rebalanceplanclaims API.
          It allows users to request generation of a rebalancing plan from the Kent service.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired rebalancing configuration
            properties:
              aggressiveModeConfig:
                description: |-
                  AggressiveModeConfig controls handling of problematic workloads during rebalancing.
                  Enables rebalancing of pods that would normally be considered too risky.
                properties:
                  aggressiveEviction:
                    description: |-
                      AggressiveEviction enables aggressive eviction mode for node drain.
                      When true, a drain timeout will be set on NodeClaims to force eviction.
                    type: boolean
                  drainTimeout:
                    description: |-
                      DrainTimeout specifies the drain timeout when AggressiveEviction is enabled.
                      If not specified, nodes drain indefinitely (graceful drain).
                      Format: duration string (e.g., "5m", "10m", "30m")
                    type: string
                  ignoreBlockingPDB:
                    description: |-
                      IgnoreBlockingPDB allows rebalancing pods blocked by PodDisruptionBudgets with 0 disruptions allowed.
                      WARNING: May violate PDB guarantees during eviction.
                    type: boolean
                  ignoreInstanceCriteria:
                    description: |-
                      IgnoreInstanceCriteria removes instance type constraints from NodePools during rebalancing.
                      Allows broader instance type selection for cost optimization.
                    type: boolean
                  ignoreLocalPersistentVolumes:
                    description: |-
                      IgnoreLocalPersistentVolumes allows rebalancing nodes with pods using local-path-provisioner PVs.
                      WARNING: May result in data loss if pods are not properly backed up.
                    type: boolean
                  ignoreProblemJobPods:
                    description: IgnoreProblemJobPods allows rebalancing Job and CronJob
                      pods.
                    type: boolean
                  ignoreProblemPodsWithoutController:
                    description: |-
                      IgnoreProblemPodsWithoutController allows rebalancing bare pods without controller.
                      WARNING: These pods will not be rescheduled automatically.
                    type: boolean
                  ignoreProblemRemovalDisabledPods:
                    description: IgnoreProblemRemovalDisabledPods allows rebalancing
                      pods marked with removal-disabled annotation.
                    type: boolean
                  ignoreZeroNodePoolDisruptionBudgets:
                    description: |-
                      IgnoreZeroNodePoolDisruptionBudgets overrides NodePool disruption budgets that allow 0 disruptions
                      with the default disruption budget (10% of nodes). This prevents nodes under active
                      zero-disruption budgets from blocking rebalancing.
                      WARNING: May violate NodePool disruption budget guarantees.
                    type: boolean
                type: object
              autoExecute:
                description: |-
                  AutoExecute determines if the generated plan should execute automatically.
                  If false, plan is generated but requires manual approval to execute.
                type: boolean
              executionPolicy:
                description: ExecutionPolicy defines policies for plan execution validation.
                properties:
                  achievedSavingsPercentageThreshold:
                    description: |-
                      AchievedSavingsPercentageThreshold is the minimum percentage of cost savings relative to the
                      original (blue) cost that must be achieved for the rebalancing to proceed.
                      Range: 0-100
                      0   = No validation (optimistic, always proceed)
                      80  = Require at least 80% of the original cost to be saved
                      100 = Require exact cost prediction (strict, may fail often)
                      The rebalancing plan will not proceed after creating the nodes if the achieved savings
                      percentage is below this threshold.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                type: object
              minSavingsPercentage:
                description: |-
                  MinSavingsPercentage is the minimum projected cost savings percentage required to proceed.
                  Range: 0-100. If 0, always rebalance (useful for spot rotation). If >0, only rebalance
                  if projected savings are greater than or equal to this percentage.
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              nodeConstraints:
                description: |-
                  NodeConstraints defines constraints for node selection during rebalancing.
                  These constraints override the default Node Disruption Budget from NodePool.
                properties:
                  maxNodes:
                    description: |-
                      MaxNodes is the maximum number of nodes to rebalance in a single operation.
                      Limits blast radius and duration.
                      Example: 10 = max 10 nodes per execution
                    format: int32
                    minimum: 1
                    type: integer
                  minAgeSeconds:
                    description: |-
                      MinAgeSeconds is the minimum node age in seconds before considering for rebalancing.
                      Prevents rebalancing freshly created nodes.
                      Example: 300 = don't touch nodes younger than 5 minutes
                    format: int64
                    minimum: 0
                    type: integer
                  minClusterSize:
                    description: |-
                      MinClusterSize is the minimum number of nodes to keep in the cluster.
                      Safety guard to prevent cluster from becoming too small.
                      Example: 3 = never go below 3 nodes
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              scope:
                description: Scope limits rebalancing to specific nodes. If empty,
                  considers entire cluster.
                properties:
                  nodeNames:
                    description: |-
                      NodeNames is a list of specific node names to consider for rebalancing.
                      If empty or nil, all nodes in the cluster are considered.
                      Mutually exclusive with NodeSelector.
                    items:
                      type: string
                    type: array
                  nodeSelector:
                    description: |-
                      NodeSelector selects nodes using Kubernetes node selector semantics.
                      Terms are ORed; requirements within a term are ANDed.
                      Mutually exclusive with NodeNames.
                    properties:
                      nodeSelectorTerms:
                        description: Required. A list of node selector terms. The
                          terms are ORed.
                        items:
                          description: |-
                            A null or empty node selector term matches no objects. The requirements of
                            them are ANDed.
                            The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                        x-kubernetes-list-type: atomic
                    required:
                    - nodeSelectorTerms
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
                x-kubernetes-validations:
                - message: nodeNames and nodeSelector are mutually exclusive
                  rule: '!(has(self.nodeNames) && size(self.nodeNames) > 0 && has(self.nodeSelector))'
              timeouts:
                description: Timeouts controls various timeout settings for plan generation
                  and execution
                properties:
                  planGenerationTimeout:
                    description: |-
                      PlanGenerationTimeout is the maximum time to wait for RebalancePlan CR creation
                      Default: 5m
                      Format: duration string (e.g., "5m", "10m", "1h")
                    type: string
                  rebalanceExecutionTimeout:
                    description: |-
                      RebalanceExecutionTimeout is the maximum time to wait for plan execution
                      Default: 1h
                      Format: duration string (e.g., "1h", "30m", "1h30m")
                    type: string
                type: object
            type: object
          status:
            description: status defines the observed state
            properties:
              completionTime:
                description: CompletionTime is the time when the claim reached a terminal
                  state (Completed or Failed).
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the request.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              errorMessage:
                description: ErrorMessage contains details if the request failed.
                type: string
              operationId:
                description: |-
                  OperationID is the Kent long-running operation ID for polling generation status.
                  Populated when request is submitted to Kent.
                type: string
              planId:
                description: PlanID is the Kent plan ID once generation completes.
                type: string
              projectedSavingsPercent:
                description: |-
                  ProjectedSavingsPercent is the percentage of cost reduction from the rebalancing plan.
                  Format: decimal string (e.g., "15.5" means 15.5% savings).
                  Populated after plan generation completes.
                type: string
              rebalanceName:
                description: |-
                  RebalanceName is the name of the created Rebalance CR.
                  Populated when Rebalance CR is created from the generated plan.
                type: string
              selectedNodes:
                description: |-
                  SelectedNodes is the list of node names selected for rebalancing after applying
                  scope filters and node constraints. Populated when the request is submitted to Kent.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              selectedNodesCount:
                description: |-
                  SelectedNodesCount is the number of nodes in SelectedNodes.
                  This field is populated automatically and exists for display purposes.
                type: integer
              skippedNodes:
                description: |-
                  SkippedNodes lists nodes that were in the rebalancing scope but could not be selected,
                  along with the reasons why each node was skipped.
                items:
                  description: SkippedNodeStatus describes a node that was in the
                    rebalancing scope but could not be selected.
                  properties:
                    name:
                      description: Name is the name of the skipped node.
                      type: string
                    reasons:
                      description: Reasons lists why the node was not selected for
                        rebalancing.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              state:
                description: State represents the current lifecycle state of the request.
                enum:
                - Pending
                - Submitted
                - Generating
                - Ready
                - Executing
                - Completed
                - Failed
                type: string
              warnings:
                description: |-
                  Warnings contains warnings from pod placement events during plan generation.
                  Each entry corresponds to a pod that could not be placed.
                items:
                  description: RebalancePlanWarning describes a warning encountered
                    during plan generation.
                  properties:
                    description:
                      description: Description is a human-readable warning message.
                      type: string
                  required:
                  - description
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
