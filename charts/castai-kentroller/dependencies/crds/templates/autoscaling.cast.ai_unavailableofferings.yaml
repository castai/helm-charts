# Code generated by controller-gen. DO NOT EDIT.
# Source declarations: ./services/kentroller/gen.go
# Regenerate with: make generate
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: unavailableofferings.autoscaling.cast.ai
spec:
  group: autoscaling.cast.ai
  names:
    kind: UnavailableOffering
    listKind: UnavailableOfferingList
    plural: unavailableofferings
    shortNames:
    - uo
    singular: unavailableoffering
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.instanceType
      name: Instance Type
      type: string
    - jsonPath: .spec.zoneId
      name: Zone ID
      type: string
    - jsonPath: .spec.capacityType
      name: Capacity
      type: string
    - jsonPath: .spec.reason
      name: Reason
      type: string
    - jsonPath: .spec.expiresAt
      name: Expires
      type: string
    - jsonPath: .spec.attempts
      name: Attempts
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha
    schema:
      openAPIV3Schema:
        description: |-
          UnavailableOffering records that a specific instance type + zone ID + capacity type
          combination is temporarily unavailable.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UnavailableOfferingSpec defines the details of an unavailable
              offering.
            properties:
              attempts:
                description: |-
                  Attempts is the number of times this offering has been marked unavailable.
                  Used to compute linear backoff TTL.
                type: integer
              capacityType:
                description: CapacityType is the capacity type of the unavailable
                  offering.
                enum:
                - spot
                - on-demand
                type: string
              cleanupAfter:
                description: |-
                  CleanupAfter is the time after which the GC controller may delete this CR.
                  Set to one backoff step beyond ExpiresAt so the CR survives past expiration
                  and can accumulate Attempts if the same offering is marked again.
                format: date-time
                type: string
              expiresAt:
                description: |-
                  ExpiresAt is the time after which this offering is no longer considered unavailable.
                  Consumers should ignore entries where ExpiresAt is in the past.
                format: date-time
                type: string
              instanceType:
                description: InstanceType is the instance type that is unavailable
                  (e.g., "m5.xlarge").
                type: string
              reason:
                description: Reason describes why the offering is unavailable.
                enum:
                - InsufficientCapacityError
                - SpotInterruptionPredicted
                - SpotInterruptionObserved
                type: string
              zoneId:
                description: |-
                  ZoneID is the provider-stable availability zone identifier for this offering.
                  For AWS this is the physical AZ ID, for example "use1-az1".
                type: string
            required:
            - capacityType
            - expiresAt
            - instanceType
            - reason
            - zoneId
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
