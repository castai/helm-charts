# Code generated by controller-gen. DO NOT EDIT.
# Source declarations: ./services/kentroller/gen.go
# Regenerate with: make generate
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: commitmentbudgets.autoscaling.cast.ai
spec:
  group: autoscaling.cast.ai
  names:
    kind: CommitmentBudget
    listKind: CommitmentBudgetList
    plural: commitmentbudgets
    shortNames:
    - cb
    singular: commitmentbudget
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.ri.instanceType
      name: Instance Type
      type: string
    - jsonPath: .spec.ri.count
      name: RI Count
      type: integer
    - jsonPath: .spec.ri.instanceFamily
      name: RI Family
      type: string
    - jsonPath: .spec.ec2InstanceSP.instanceFamily
      name: SP Family
      type: string
    - jsonPath: .spec.computeSP.commitmentAmount
      name: Commitment
      type: string
    - jsonPath: .status.matchedNodePools
      name: Matched NodePools
      type: integer
    - jsonPath: .status.conditions[?(@.type=='CompatibleNodePools')].reason
      name: Compatible
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha
    schema:
      openAPIV3Schema:
        description: CommitmentBudget is the Schema for the commitmentbudgets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              CommitmentBudgets are RI or SP with different specs
              Storing them together as controller logic is expected to be mostly reused
            properties:
              budgetID:
                type: string
              computeSP:
                description: ComputeSP holds Compute Savings Plan budget spec
                properties:
                  commitmentAmount:
                    description: CommitmentAmount is the hourly commitment in USD.
                    type: string
                  familyCPUPrices:
                    additionalProperties:
                      description: FamilyPricing holds the cheapest per-vCPU pricing
                        for an instance family.
                      properties:
                        instanceType:
                          description: InstanceType that produced the cheapest per-vCPU
                            price (e.g. "m5.metal").
                          type: string
                        pricePerCPU:
                          description: |-
                            PricePerCPU is the cheapest hourly per-vCPU price in USD across instance
                            types in this family for the SP offering.
                          type: string
                      required:
                      - pricePerCPU
                      type: object
                    description: |-
                      FamilyCPUPrices maps instance family (e.g. "m5") to the cheapest per-vCPU
                      pricing for that family under this SP offering.
                    type: object
                  offeringId:
                    description: OfferingID is the ID of the offering that this budget
                      belongs to.
                    type: string
                required:
                - commitmentAmount
                type: object
              ec2InstanceSP:
                description: EC2InstanceSP holds EC2 Instance Savings Plan budget
                  spec
                properties:
                  commitmentAmount:
                    description: CommitmentAmount is the hourly commitment in USD.
                    type: string
                  instanceFamily:
                    description: InstanceFamily is the instance family (e.g., "m5").
                    type: string
                  offeringId:
                    description: OfferingID is the ID of the offering that this budget
                      belongs to.
                    type: string
                  pricePerCPU:
                    description: |-
                      PricePerCPU is the cheapest hourly per-vCPU price in USD for the instance
                      family under this SP offering.
                    type: string
                required:
                - commitmentAmount
                - instanceFamily
                type: object
              ri:
                description: RI holds Reserved Instance budget spec
                properties:
                  availabilityZone:
                    description: AvailabilityZone - empty for regional RIs, filled
                      for zonal RIs
                    type: string
                  count:
                    description: Count is the number of reserved instance nodes (zonal
                      RIs).
                    minimum: 0
                    type: integer
                  cpu:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Resource limits for regional RI family
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  instanceFamily:
                    description: Family in case of regional RIs
                    type: string
                  instanceType:
                    description: InstanceType is specific instance type in case of
                      zonal RIs
                    type: string
                  memory:
                    anyOf:
                    - type: integer
                    - type: string
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              type:
                description: Type of commitment budget.
                enum:
                - reserved-instance
                - compute-savings-plan
                - ec2-instance-savings-plan
                type: string
            required:
            - budgetID
            - type
            type: object
          status:
            description: CommitmentBudgetStatus defines the observed state of a CommitmentBudget.
            properties:
              conditions:
                description: Conditions represent the current state of the CommitmentBudget.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              matchedNodePools:
                description: MatchedNodePools is the number of customer NodePools
                  that match this budget.
                type: integer
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
