global:
  tolerations:
    - effect: NoSchedule
      key: global-taint
      operator: Equal
      value: "true"

castai:
  apiKey: "test-api-key"
  clusterID: "test-cluster-id"

openshift:
  scc:
    enabled: true
    priority: 10
    useRestrictedProfile: true
