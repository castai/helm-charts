{{- if or .Values.apiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "name" . }}
  labels:
    {{- include "labels" . | nindent 4 }}
data:
  API_KEY: {{ .Values.apiKey | b64enc | quote }}
  {{- if and (ne .Values.database.username "") (ne .Values.database.password "") }}
  DATABASE_USERNAME: {{ .Values.database.username | b64enc | quote }}
  DATABASE_PASSWORD: {{ .Values.database.password | b64enc | quote }}
  {{- end }}
{{- end }}
